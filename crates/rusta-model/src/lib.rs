#![deny(warnings)]
/// Burn backbone + heads + collator (stub)
pub struct ByteLm;
impl ByteLm {
    pub fn explain(_bytes: &[u8]) -> String {
        // TODO: connect to real model
        "explain(stub)".into()
    }
}
